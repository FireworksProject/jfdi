server {
    server_name localhost;
    root /usr/share/nginx/html;
    location ~ \.php$ {
        # try_files $uri =404;
        fastcgi_pass unix:/var/run/php5-fpm-default_nginx.socket;
        # fastcgi_index index.php;
        include fastcgi_params;
    }
}
